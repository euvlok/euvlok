db_path = "{{ .chezmoi.homeDir }}/.local/share/atuin/history.db"
key_path = "{{ .chezmoi.homeDir }}/.local/share/atuin/key"

sync_address = "http://127.0.0.1:8888"
auto_sync = true

search_mode = "fuzzy"
filter_mode_shell_up_key_binding = "directory"
filter_mode = "global"
enter_accept = false

style = "compact"
inline_height = 40
show_preview = true

[daemon]
enabled = true
autostart = true
socket_path = "{{ .chezmoi.homeDir }}/.local/share/atuin/atuin.sock"
sync_frequency = 300
